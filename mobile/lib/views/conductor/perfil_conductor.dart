import 'package:flutter/material.dart';

class PerfilConductor extends StatelessWidget {
  const PerfilConductor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pantalla de Perfil del Conductor"),
    );
  }
}
