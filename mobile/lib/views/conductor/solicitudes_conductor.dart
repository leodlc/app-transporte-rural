import 'package:flutter/material.dart';

class SolicitudesConductor extends StatelessWidget {
  const SolicitudesConductor({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Pantalla de Solicitudes del Conductor"),
    );
  }
}
